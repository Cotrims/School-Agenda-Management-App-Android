<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_lista">

    <fragment
        android:id="@+id/navigation_editar"
        android:name="br.cotuca.agendaaluno.ui.editar.EditarFragment"
        android:label="@string/title_editar"
        tools:layout="@layout/fragment_editar" />

    <fragment
        android:id="@+id/navigation_adicionar"
        android:name="br.cotuca.agendaaluno.ui.adicionar.AdicionarFragment"
        android:label="@string/title_adicionar"
        tools:layout="@layout/fragment_adicionar" />

    <fragment
        android:id="@+id/navigation_lista"
        android:name="br.cotuca.agendaaluno.ui.lista.ListaFragment"
        android:label="@string/title_lista"
        tools:layout="@layout/fragment_lista" />
</navigation>